version: '3'
services: 
  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/app
    ports: 
      - "3000:80" 
  backend:
    build: ./backend
    volumes:
      - ./backend:/usr/src/app
    ports:
      - "8080:8080"